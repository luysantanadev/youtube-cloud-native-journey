community:
  enabled: true

setAdminPassword:
  newPassword: "Workshop_1_sonar"
  currentPassword: "admin"

monitoringPasscode: "sonarWorkshop123"

ingress:
  enabled: true
  ingressClassName: traefik
  hosts:
    - name: sonarqube.monitoramento.local
      path: /
      pathType: Prefix

prometheusMonitoring:
  podMonitor:
    enabled: true
    interval: 30s
    labels:
      release: kube-prometheus-stack

resources:
  requests:
    cpu: "400m"
    memory: "2048Mi"
  limits:
    cpu: "1000m"
    memory: "2560Mi"

persistence:
  enabled: true
  size: 2Gi

initSysctl:
  enabled: true

elasticsearch:
  bootstrapChecks: false

# Desabilita o PostgreSQL embutido do chart e aponta para o externo (manifest.yaml)
postgresql:
  enabled: false

jdbcOverwrite:
  enable: true
  jdbcUrl: "jdbc:postgresql://sonarqube-postgresql:5432/sonarqube"
  jdbcUsername: "sonarqube"
  jdbcSecretName: "sonarqube-db-credentials"
  jdbcSecretPasswordKey: "postgres-password"
